import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'proyecto.noname',
  appName: 'proyecto-noname',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
